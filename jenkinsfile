pipeline {
    agent any

    stages {
stage('Verify POM') {
    steps {
        bat 'type pom.xml'
    }
}
stage('Effective POM') {
    steps {
        bat 'mvn help:effective-pom'
    }
}
    }

    post {
        always {
            junit '**/target/surefire-reports/*.xml'
        }
    }
}
